export interface Patient {
  address: string;
  email: string;
  name: string;
  phoneNumber: number;
  photoPath: string;
}

export interface PatientRequest {
  address: string;
  email: string;
  name: string;
  phoneNumber: number;
}
